# ── OpenAI (TTS 오디션용) ──
openai>=1.0,<2.0

# ── YouTube Upload ──
google-api-python-client>=2.0,<3.0
google-auth-oauthlib>=1.0,<2.0
google-auth-httplib2>=0.2,<1.0

# ── ModelArk 영상 생성 ──
requests>=2.31,<3.0

# ── 채널 브랜딩 이미지 (scripts/brand.py) ──
Pillow>=10.0,<12.0

# ── Utilities ──
python-dotenv>=1.0,<2.0
rich>=13.0,<14.0
